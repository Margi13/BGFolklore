@model IList<ManageFeedbackViewModel>
@{
    ViewData["Title"] = "Управление на сигналите";
}

<header class="page-header admin-header m-0">
    <div class="rt-container op-80 bg-brown">
        <h2>@ViewData["Title"]</h2>
        <p>@*Там, където музиката се включва, а заобикалящият ни свят се изключва!*@</p>
    </div>
    <div class="nav-container horizontal-top">
        <div class="page-navbar horizontal-navbar">
            <ul>
                <li class="w-auto">
                    <a class="area-link op-80 bg-brown" href="#" id="allFeeds">Всички</a>
                </li>
                <li class="w-auto">
                    <a class="area-link op-80 bg-brown" href="#" id="newFeeds">Нови сигнали</a>
                </li>
                <li class="w-auto">
                    <a class="area-link op-80 bg-brown" href="#" id="oldFeeds">Прочетени сигнали</a>
                </li>
            </ul>
        </div>
    </div>
</header>

<section class="p-0" id="table-container">
    <table class="calendar-table">
        <thead>
            <tr class="text-center">
                <th class="table-header" title="Подреди по потребителско име">
                    <a asp-action="ShowAllEvents"
                       asp-route-sortBy="@ViewData["UserName"]">
                        Подател
                    </a>
                </th>
                <th>Дата на подаване</th>
                <th class="table-header number-cell" title="Подреди по брой събития">
                    <a asp-action="ShowAllEvents"
                       asp-route-sortBy="@ViewData["EventName"]">
                        Име на събитието
                    </a>
                </th>
                <th>Описание на сигнал</th>
                <th class="table-header number-cell" title="Подреди по брой събития">
                    <a asp-action="ShowAllEvents"
                       asp-route-sortBy="@ViewData["Status"]">
                        Статус на сигнал
                    </a>
                </th>
                <th>Управление</th>
            </tr>
        </thead>
        <tbody>
            @Html.DisplayForModel();
        </tbody>
    </table>
</section>

@section CSS {
    <link rel="stylesheet" href="~/css/admin-page.css" />
    <link rel="stylesheet" href="~/css/calendar.css" />
}
@section Scripts {
    <script type="text/javascript">
        $(".area-link").click(function () {
            let rows = document.getElementsByClassName('info-row');
            for (let r of rows) {
                if (this.id.includes("allFeeds") || r.classList.contains(this.id)) {
                    r.classList.remove("hidden");
                } else {
                    r.classList.add("hidden");
                }
            }
        });
    </script>
}